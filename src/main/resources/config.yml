# 调度配置不包含公告内容, 公告列表请写入 tips.yml
announcements:
  # 是否启用自动公告定时任务, 关闭后仍可使用 /tips next 手动发送随机公告
  enabled: true

  # 自动公告间隔秒数, 小于 5 时会自动提升到 5
  interval-seconds: 300

  # 启动或重载后首次自动公告延迟秒数
  # 小于 1 时会自动提升到 1
  initial-delay-seconds: 30

  # 是否将每次发送公告的纯文本内容写入控制台
  send-to-console: true
